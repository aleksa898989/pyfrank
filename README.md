# pyfrank

A small CLI tool that reports word and letter frequency stats for a text file (Frankenstein, by default).

## Run locally

```bash
python3 main.py
```

## Run with Docker

```bash
docker build -t bookbot -f Dockerfile.py .
docker run bookbot
```
